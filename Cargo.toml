[package]
name = "go-init"
description = "A program that creates or initialises a golang module and git."
version = "1.0.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
